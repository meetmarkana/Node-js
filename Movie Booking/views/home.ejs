<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie Booking</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #000;
            color: #fff;
        }
        header {
            background-color: #222;
            padding: 10px 0;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
        }
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        .navbar .logo {
            color: white;
            font-size: 1.8rem;
            font-weight: bold;
            text-decoration: none;
        }
        .navbar ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
            display: flex;
        }
        .navbar ul li {
            margin-left: 20px;
        }
        .navbar ul li a {
            color: white;
            text-decoration: none;
            font-size: 1rem;
            padding: 8px 12px;
            transition: background-color 0.3s ease;
        }
        .navbar ul li a:hover {
            background-color: #444;
            border-radius: 5px;
        }
        .search-bar {
            display: flex;
            align-items: center;
        }
        .search-bar input {
            padding: 8px;
            border: none;
            border-radius: 4px;
            outline: none;
        }
        .search-bar button {
            background-color: #ff4d4d;
            border: none;
            color: white;
            padding: 8px 12px;
            border-radius: 4px;
            margin-left: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .search-bar button:hover {
            background-color: #e60000;
        }
        .auth-buttons {
            display: flex;
        }
        .auth-buttons button {
            background-color: #ff4d4d;
            border: none;
            color: white;
            padding: 8px 15px;
            margin-left: 10px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .auth-buttons button:hover {
            background-color: #e60000;
        }
        .card-container {
            display: flex;
            flex-wrap: wrap; 
            gap: 20px; 
            justify-content:flex-start; 
        }       
        .movie-card {
            background-color: #faf7f7; 
            border-radius: 10px;     
            padding: 20px;
            width: 300px; 
            text-align: left;
            transition: transform 0.2s, box-shadow 0.2s; 
        }
        .movie-card:hover {
            transform: scale(1); 
            box-shadow: 0 8px 16px rgba(255, 255, 255, 0.3); 
        }
        .movie-card .poster {
            width: 100%; 
            height: auto;
            border-radius: 5px;
            margin-bottom: 15px; 
        }
        .movie-card .title {
            font-size: 1.5em;
            margin: 10px 0;
            color: #0b0b0b;
        }
        .movie-card .genre {
            font-size: 1em;
            color: #0b0b0b; 
            margin-bottom: 15px;
        }
        .card-actions {
            display: flex;
            justify-content: space-between; 
        }
        .card-button {
            background-color: #ff4757; 
            border: none;
            color: #fff;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
            transition: background-color 0.2s;
            text-decoration: none; 
        }
        .card-button:hover {
            background-color: #e84118; 
        }
        .card-button.edit:hover {
            background-color: #1eae5f;/
        }
    </style>
</head>

<body>
    <header>
        <div class="navbar">
            <a href="#" class="logo">MovieBooking</a>
            <ul>
                <li><a href="#">Movies</a></li>
                <li><a href="#">Events</a></li>
                <li><a href="#">Plays</a></li>
                <li><a href="#">Sports</a></li>
            </ul>

            <div class="search-bar">
                <input type="text" placeholder="Search for Movies, Events...">
                <button type="button">Search</button>
            </div>

            <div class="auth-buttons">
                <a href="/addData"><button>ADD</button></a>
                <button>Sign Up</button>
            </div>
        </div>
        
    </header>
    <div class="card-container">
        <% data.map((el, i) => { %>
        <div class="movie-card">
            <img src="<%= el.image %>" alt="<%= el.name %>" class="poster" width="30%">

            <h3 class="title"><%= el.name %></h3>
            <h3 class="genre"><%= el.type %></h3>
            <h3 class="genre"><%= el.description %></h3>
            <h3 class="genre"><%= el.price %> ₹</h3>
            <div class="card-actions">
                <a href="/deletemovie?id=<%= el.id %>"><button class="card-button">Delete</button></a>
                <a href="/editdmovie?id=<%= el.id %>"><button class="card-button">Edit</button></a>
            </div>
        </div>
        <% }) %>
    </div>
    
</body>
</html>